# Security

EGSA Suite is a local desktop application. The Google Earth integration starts an embedded HTTP
server bound to `127.0.0.1` only; it is not intended to expose a network service to other devices.

Do not publish sensitive datasets, credentials or private paths in bug reports. If a security issue
could expose local files or execute untrusted input, report it privately to the repository maintainer
before opening a public issue.
